export default function Home() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Streakr AFL (Vercel Build)</h1>
      <p>After deploy, test <code>/api/diag-admin</code> — should return ok ✅</p>
    </main>
  );
}
